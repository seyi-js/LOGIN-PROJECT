<div class="row mt-5">
    <div class="col-md-6 n-auto">
        <div class="card card-body">
            <h1 class="text-center mb-3">Change Password</h1>
                                <%- include ("./partials/messages") %>
            <form action="/users/changepass" method="POST">
                <div class="form-group">
                    <label for="password">Old Password</label>
                    <input type="password" id="password" name="password" class="form-control"
                        placeholder="Create Password" value="<%= typeof email != 'undefined' ? password : '' %>" />
                </div>
                <div class="form-group">
                    <label for="password2">New Password</label>
                    <input type="password" id="password2" name="password2" class="form-control"
                        placeholder="Create Password" value="<%= typeof email != 'undefined' ? password2 : '' %>" />
                </div>
                <div class="form-group">
                    <label for="password3">Confirm New Password</label>
                    <input type="password" id="password3" name="password3" class="form-control"
                        placeholder="Confirm Password" value="<%= typeof email != 'undefined' ? password3 : '' %>" />
                </div>
                <button type="submit" class="btn btn-primary btn-block">
                    submit
                </button>
            </form>
            
        </div>
    </div>
</div>
</div>
</div>